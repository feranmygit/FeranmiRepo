const text = document.getElementById('text');

function typeText() {
    let index= 0;
    const typingInterval = setInterval(() => {
        tyingText.style.display = 'inline';
        tyingText.textCo
    })
}